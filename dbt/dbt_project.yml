name: "hubspot_dbt"
version: "1.0"
config-version: 2

profile: "hubspot_elt"

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  hubspot_dbt:
    +materialized: view

    staging:
      +schema: staging
      +tags: ["staging"]
      hubspot:                 # 👈 ajoute ce bloc
        +tags: ["staging"]

    intermediate:
      +schema: intermediate
      +tags: ["intermediate"]
      hubspot:                 # 👈 et celui-ci
        +tags: ["intermediate"]

    marts:
      +schema: marts
      +tags: ["marts"]

    kpi:
      +schema: kpi
      +tags: ["kpi"]

